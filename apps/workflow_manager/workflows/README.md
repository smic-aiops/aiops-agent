# workflows/（移行済み）

`apps/workflow_manager/workflows/` は互換のため残していますが、ワークフロー定義（JSON）は機能別ディレクトリへ移動しました。

- Workflow Catalog: `apps/workflow_manager/workflow_catalog/workflows/`
- Service Request: `apps/workflow_manager/service_request/workflows/`

