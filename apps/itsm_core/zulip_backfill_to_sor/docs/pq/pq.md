# PQ（性能適格性確認）: Zulip Backfill to SoR

## 目的

Zulip の走査量が大きい場合に、実行時間・API 制限・DB 負荷が許容範囲であることを確認する（必要時）。

## 観点（例）

- `--page-size` / `--max-pages` により走査量を制御できる
- scan/execute を分離して段階的に運用できる
- 冪等キーにより再実行しても重複しない

## 証跡（evidence）

- 実行ログ（所要時間、走査件数、投入件数）

