.terraform/
terraform.tfstate
terraform.tfstate.*
*.tfvars
*.tfvars.*
*.tfstate
*.tfstate.*
tfplan
tfplan*
tfplan_out
tfplan_cli
tfplan_unpack/

# Local image tarballs
images/
modules/stack/templates/*.zip
docker/sulu/source/

# OQ / test evidence (local only)
evidence/
!apps/**/docs/oq/evidence/
!apps/**/docs/oq/evidence/**

# IQ test outputs (local only)
apps/workflow_manager/data/iq_test_*.jsonl

# Realm overlays (org-specific / generated artifacts)
# - Canonical: vendor/<name_prefix>/apps/<app>/realms/<realm_key>/...
# - Legacy fallback: apps/<app>/realms/<realm_key>/...
# - Keep upstream repo clean; manage realm overlays outside tracked files.
apps/**/realms/**
!apps/**/realms/_template/
!apps/**/realms/_template/**

# Local logs
logs/

# CLI history / OS cruft
.DS_Store

# Python cache
__pycache__/

vendor/
